# Security settings
audit=true
audit-level=moderate
fund=false

# Lock dependencies
package-lock=true

# Force resolution of vulnerable packages
force=true

# Ensure consistent dependency resolution
save-exact=false
description=false
